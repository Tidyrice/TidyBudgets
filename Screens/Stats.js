import { StyleSheet, Text, View, SafeAreaView } from 'react-native';

//shows spending for each category (current month). Difference from target budget.
export default function Stats( {navigation} ) {
    return(

        <SafeAreaView>
            <Text>STATS SCREEN</Text>
        </SafeAreaView>
        
    );
}